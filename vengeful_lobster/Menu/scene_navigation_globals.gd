extends Node
## Stores references to scenes to easily traverse them.

var start_menu:String = ("uid://lx4in05f0ers")
var game_level:String = ("uid://ck3t5e5ta252n")

var stored_scene:String = ""

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
